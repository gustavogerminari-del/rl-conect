name: Apply friendly validation

on:
  push:
    branches: [agent/google-calendar-meet]
    paths:
      - scripts/apply-friendly-validation.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/google-calendar-meet
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python scripts/apply-friendly-validation.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/views/DepartamentoPessoalView.tsx src/components/views/HeadhunterView.tsx
          git diff --cached --quiet && exit 0
          git commit -m "fix: guide users through validation requirements"
          git push origin HEAD:agent/google-calendar-meet
